#include "main.h"
/**
 * int_abs - function that computes the absolute value of an integer.
 * @n: the character to be checked
 * Return: absolute value of int
 */
int _abs(int n)
{
if (n >= 0)
	{
		return (n);
	}
	return (-n);
}

#include <stdio.h>
/**
 * main -Entry point
 * Description: prints all numbers of base 16 in lowercase
 * followed by a new line.
 * Return: Always 0 (success)
 */
int main(void)
{
	int a;
	char ch;

	for (a = 48; a < 58; a++)
	{
		putchar(n);
	}
	for (ch = 'a'; ch <= 'f'; ch++)
	{
		putchar(ch);
	}
	putchar('\n');
	return (0);
}

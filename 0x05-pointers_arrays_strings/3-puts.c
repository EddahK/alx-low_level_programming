#include "main.h"

/**
 * _puts - a function that prints a string, followed by a new line
 * @str: string to be printed
 * Return: 0.
 */
void _puts(char *str)
{
str = "*str++";
putchar(str);
putchar('\n');
}
